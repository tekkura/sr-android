# Audit Event 68885bfdf50875ce8375570f52735893873b5c409a450ae4c9baaedba5e4566c

- Timestamp: 2026-03-11T02:26:44Z
- Actor: Ma7moud3ly
- Event: issue_comment.edited
- Target URL: https://github.com/tekkura/sr-android/issues/154#issuecomment-4031833327

## Raw Event

```json
{
  "timestamp": "2026-03-11T02:26:44Z",
  "event_ts": "2026-03-11T02:26:27Z",
  "repo": "tekkura/sr-android",
  "event_name": "issue_comment",
  "action": "edited",
  "actor": "Ma7moud3ly",
  "run_id": "22933770749",
  "run_attempt": "1",
  "sender": {
    "login": "Ma7moud3ly",
    "html_url": "https://github.com/Ma7moud3ly"
  },
  "issue": {
    "number": 154,
    "title": "Build equivalent demo apps in shortlisted Python Android frameworks",
    "state": "open",
    "html_url": "https://github.com/tekkura/sr-android/issues/154",
    "milestone": {
      "number": 10,
      "title": "pythonDroid"
    }
  },
  "comment": {
    "id": 4031833327,
    "html_url": "https://github.com/tekkura/sr-android/issues/154#issuecomment-4031833327",
    "created_at": "2026-03-10T14:28:40Z",
    "updated_at": "2026-03-11T02:26:27Z",
    "body": "@topherbuckley Are you ok with this Structure?\n\n```\napp\n│\n├── src/main/java/.../MainActivity.kt\n│\n└── src/main/python\n    ├── abcvlib.py\n    └── main.py\n```\n\n### MainActivity \n```kotlin\nclass MainActivity : AbcvlibActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n    }\n\n    override fun onSerialReady(usbSerial: UsbSerial) {\n        super.onSerialReady(usbSerial)\n        initPython()\n    }\n\n    private fun initPython() {\n        if (!Python.isStarted()) {\n            Python.start(AndroidPlatform(this))\n        }\n\n        val py = Python.getInstance()\n        val setupModule = py.getModule(\"abcvlib\")\n        // inject variables to python\n        setupModule.put(\"outputs\", outputs)\n        setupModule.put(\"loop_delay\", 0.005) // 5 ms\n\n        lifecycleScope.launch(Dispatchers.Default) {\n            try {\n                setupModule.callAttr(\"run\")\n            } catch (e: PyException) {\n                Logger.e(\"MainActivity\", \"Python error: ${e.message}\")\n            }\n        }\n    }\n}\n```\n### python/abcvlib.py\nThis module acts as the bridge between Android and the Python control logic.\n```python\nimport time\nimport builtins\nimport main\n\noutputs = None\nloop_delay = 0.005 \n\n\ndef run():\n    builtins.outputs = outputs  # expose AbcvlibActivity.outputs to main.py\n\n    main.setup()\n    print(\"setup done\")\n\n    print(\"start main-loop with dealy\", loop_delay)\n    while True:\n        main.loop()\n        time.sleep(loop_delay)\n```\n\n### python/main.py\nThe actual app logic.\n```python\nspeed = 0.0\nincrement = 0.4\n\ndef setup():\n    print(\"Hello World!\")\n\ndef loop():\n    global speed, increment\n    outputs.setWheelOutput(speed, speed, False, False)\n\n    if speed >= 1.0 or speed <= -1.0:\n        increment = -increment\n    speed += increment\n```\n"
  },
  "milestone": null,
  "changes": {
    "body": {
      "from": "@topherbuckley Are you ok with this Structure?\n\n```\napp\n│\n├── src/main/java/.../MainActivity.kt\n│\n└── src/main/python\n    ├── abcvlib.py\n    └── main.py\n```\n\n### MainActivity \n```kotlin\nclass MainActivity : AbcvlibActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n    }\n\n    override fun onSerialReady(usbSerial: UsbSerial) {\n        super.onSerialReady(usbSerial)\n        initPython()\n    }\n\n    private fun initPython() {\n        if (!Python.isStarted()) {\n            Python.start(AndroidPlatform(this))\n        }\n\n        val py = Python.getInstance()\n        val setupModule = py.getModule(\"abcvlib\")\n        // inject variables to python\n        setupModule.put(\"outputs\", outputs)\n        setupModule.put(\"loop_delay\", 0.005) // 5 ms\n\n        lifecycleScope.launch(Dispatchers.Default) {\n            try {\n                setupModule.callAttr(\"run\")\n            } catch (e: PyException) {\n                Logger.e(\"MainActivity\", \"Python error: ${e.message}\")\n            }\n        }\n    }\n}\n```\n### python/abcvlib.py\nThis module acts as the bridge between Android and the Python control logic.\n```python\nimport time\nimport builtins\nimport main\n\noutputs = None\nloop_delay = 0.010\n\n\ndef run():\n    builtins.outputs = outputs  # expose AbcvlibActivity.outputs to main.py\n\n    main.setup()\n    print(\"setup done\")\n\n    print(\"start main-loop with dealy\", loop_delay)\n    while True:\n        main.loop()\n        time.sleep(loop_delay)\n```\n\n### python/main.py\nThe actual app logic.\n```python\nspeed = 0.0\nincrement = 0.4\n\ndef setup():\n    print(\"Hello World!\")\n\ndef loop():\n    global speed, increment\n    outputs.setWheelOutput(speed, speed, False, False)\n\n    if speed >= 1.0 or speed <= -1.0:\n        increment = -increment\n    speed += increment\n```\n"
    }
  },
  "severity": "INFO",
  "target_url": "https://github.com/tekkura/sr-android/issues/154#issuecomment-4031833327",
  "event_id": "68885bfdf50875ce8375570f52735893873b5c409a450ae4c9baaedba5e4566c"
}
```

## Field Diffs

### body

```diff
diff --git a/tmp/tmp.Qu9tcrvzA9/before b/tmp/tmp.Qu9tcrvzA9/after
index 86bdebe2..bc387886 100644
--- a/tmp/tmp.Qu9tcrvzA9/before
+++ b/tmp/tmp.Qu9tcrvzA9/after
@@ -52,7 +52,7 @@ import builtins
 import main
 
 outputs = None
-loop_delay = 0.010
+loop_delay = 0.005 
 
 
 def run():
```
