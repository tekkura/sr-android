# Audit Event c781b1046b55f815fc1edc7818bb837f2a4902aba4ecf558babd642ebe16d40b

- Timestamp: 2026-04-11T08:38:38Z
- Actor: topherbuckley
- Event: issue_comment.created
- Target URL: https://github.com/tekkura/sr-android/pull/240#issuecomment-4229120646

## Raw Event

```json
{
  "timestamp": "2026-04-11T08:38:38Z",
  "event_ts": "2026-04-11T08:38:22Z",
  "repo": "tekkura/sr-android",
  "event_name": "issue_comment",
  "action": "created",
  "actor": "topherbuckley",
  "run_id": "24278791146",
  "run_attempt": "1",
  "sender": {
    "login": "topherbuckley",
    "html_url": "https://github.com/topherbuckley"
  },
  "issue": {
    "number": 240,
    "title": "Benchmark production motor command latency",
    "state": "open",
    "html_url": "https://github.com/tekkura/sr-android/pull/240",
    "milestone": null
  },
  "pull_request": null,
  "comment": {
    "id": 4229120646,
    "html_url": "https://github.com/tekkura/sr-android/pull/240#issuecomment-4229120646",
    "created_at": "2026-04-11T08:38:22Z",
    "updated_at": "2026-04-11T08:38:22Z",
    "body": "> * I think 100 iterations is way too small of a testing pool\r\n> \r\n> * Unfortunately migration to atomic integer brings back the problem we have discussed before - if previous iteration times out and the next starts before last one finished processing, some discrepancies in results will arise since UsbSerial and LatencyBenchmark only know about current iteration so they assume anything that happens while it is active is tied to it. That was the entire reason I have decided to encode iteration number into the packet itself\r\n\r\nAgreed on both points.\r\n\r\n  For the sample size: I updated the benchmark so measured iterations default to 1000 again, while still allowing\r\n  overrides via instrumentation runner args. That lets CI or quick hardware checks run something smaller, e.g.\r\n  `measuredIterations=100`, without making 100 the benchmark default.\r\n\r\n  For the `AtomicInteger` concern: you are right that it is not a robust correlation mechanism if we continue after a\r\n  timeout. I changed timeout behavior so the benchmark now fails immediately after a 1000 ms timeout instead of\r\n  logging and continuing. That means we should no longer enter the bad state where iteration N times out, iteration\r\n  N+1 starts, and the late response from N is attributed to N+1.\r\n\r\n  So the current invariant is: this benchmark assumes a synchronous single-command-in-flight loop. `currentIteration`\r\n  is only the active in-flight iteration marker, not a protocol-level correlation ID. If a response does not arrive\r\n  within the timeout, the run is invalid and aborts.\r\n\r\n  I still think we should avoid encoding iteration IDs into the motor payload for the hardware benchmark, because that\r\n  bypasses or distorts the production `setMotorLevels(Float, Float, ...)` path. If we need to continue after timeouts\r\n  or support robust response attribution, the right long-term fix is a real request/sequence ID in the protocol that\r\n  firmware echoes back.\r\n"
  },
  "milestone": null,
  "changes": null,
  "severity": "INFO",
  "target_url": "https://github.com/tekkura/sr-android/pull/240#issuecomment-4229120646",
  "event_id": "c781b1046b55f815fc1edc7818bb837f2a4902aba4ecf558babd642ebe16d40b"
}
```
