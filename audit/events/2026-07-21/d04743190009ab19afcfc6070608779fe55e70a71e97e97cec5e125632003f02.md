# Audit Event d04743190009ab19afcfc6070608779fe55e70a71e97e97cec5e125632003f02

- Timestamp: 2026-07-21T08:56:38Z
- Actor: topherbuckley
- Event: issue_comment.created
- Target URL: https://github.com/tekkura/sr-android/pull/284#issuecomment-5032032587

## Raw Event

```json
{
  "timestamp": "2026-07-21T08:56:38Z",
  "event_ts": "2026-07-21T08:56:19Z",
  "repo": "tekkura/sr-android",
  "event_name": "issue_comment",
  "action": "created",
  "actor": "topherbuckley",
  "run_id": "29816231248",
  "run_attempt": "1",
  "sender": {
    "login": "topherbuckley",
    "html_url": "https://github.com/topherbuckley"
  },
  "issue": {
    "number": 284,
    "title": "Stop background serial state polling",
    "state": "open",
    "html_url": "https://github.com/tekkura/sr-android/pull/284",
    "milestone": null
  },
  "pull_request": null,
  "comment": {
    "id": 5032032587,
    "html_url": "https://github.com/tekkura/sr-android/pull/284#issuecomment-5032032587",
    "created_at": "2026-07-21T08:56:19Z",
    "updated_at": "2026-07-21T08:56:19Z",
    "body": "> Actually, I can't help but be concerned about the removal of background polling. This would be fine if firmware doesn't change state automatically or if we don't need to make any Android-side decisions based on immediate firmware state. Seems like both aren't true\r\n> \r\n> Here's Codex's take on this: Firmware state does change without Android commands: encoder counts, charger/wireless state, battery/fuel-gauge readings, and motor faults are all read from hardware in get_state() or updated by interrupts. Android only sees those after a serial response, and the firmware does not push status asynchronously. With the current wait-forever loop in libs/abcvlib/src/main/java/jp/oist/abcvlib/util/SerialCommManager.kt:63, start(delay = ...) no longer generates those responses while idle.\r\n> \r\n> For the current apps/ examples, most normal control paths keep sending setWheelOutput(...), so status still updates as a side effect of motor-command responses. But the regression is real for any monitor-only or idle period, and Android does make status-based decisions: comprehensiveDemo uses charger state, battery voltage, and wheel speed for behavior/ balancing decisions.\r\n\r\nI agree with the assessment in general, but some missing information from the argument is that the whole project is supposed to enable ease of reinforcement learning algorithms. In all algorithms I'm aware of, one expects a deterministic way to get the state and a deterministic way to select the action. So any sort of background uncontrolled fetching or changing of state is actually a bad idea. It would be much easier to work with if the user controls the state fetching/modification every timestep/loop. Even for \"monitor-only\" apps, they can always just repeat the same setWheelOutput commands with 0,0 for both speeds and the result is the same as far as I understand. \r\n\r\nFurthermore, most algorithms expect a fixed time step. I realize that either way on this PR, that will not be the case, but having a single call for the loop rather than the queuing and decision tree for GET_STATE vs SET_MOTOR_LEVEL was causing a lot of noise in this time step. Since the currently used information from GET_STATE was already in the returned information from SET_MOTOR_LEVEL, this PR aims to unify these calls to one. GET_STATE will still be preserved but only for verbose debugging information where we no longer care about the RTT. There are a bunch of interrupts and otherwise verbose information in the firmware that the Android side doesn't need to know about for normal applications, so it is not included in the returned information from SET_MOTOR_LEVEL in order to reduce the RTT. Conversely, when this verbose information is warranted, GET_STATE (or otherwise can provide that information). If this was left as the default call, the RTT would vary greatly from call to call, which is not desirable.  \r\n\r\n"
  },
  "milestone": null,
  "changes": null,
  "severity": "INFO",
  "target_url": "https://github.com/tekkura/sr-android/pull/284#issuecomment-5032032587",
  "event_id": "d04743190009ab19afcfc6070608779fe55e70a71e97e97cec5e125632003f02"
}
```
